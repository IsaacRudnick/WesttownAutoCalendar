/**
 * console.log a specified number of newlines
 * @param {int} count number of newlines to output
 */
async function newline(count) {
  console.log("\n".repeat(count));
}
export default { newline };
